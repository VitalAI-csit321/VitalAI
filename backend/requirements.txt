# Bounded, unlike the rest of this file: app/auth/rbac_registry.py walks
# fastapi.routing._IncludedRouter, a private class. Both bounds were checked
# against the actual wheels; the ceiling forces a re-verify (see that module's
# docstring) instead of letting an unpinned install break the RBAC tests.
fastapi>=0.139.2,<0.142.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Auth
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.0
python-multipart>=0.0.9
email-validator>=2.0.0

# LLM abstraction (Phase 0 — providers swap via env)
langchain>=0.2.0
langchain-community>=0.2.0
langchain-aws>=0.1.0

# Rate limiting
slowapi>=0.1.9

# Phase 3 — orchestration, RAG, observability
langgraph>=0.1.0
langsmith>=0.1.0
langchain-postgres>=0.0.6
sentence-transformers>=3.0.0
einops>=0.7.0  # required by nomic-embed-text-v1's remote modeling code

# Matthew's ingestion pipeline (Loader/Cleaner/Chunker), pinned to what CI's
# ruff drift already burned once (see requirements.lock note); floors only,
# same class of risk as ruff until this repo installs from the lock file.
ray>=2.56.0
pypdf>=6.14.2

# Object storage (MinIO/S3), Phase 5 clinical upload
boto3>=1.43.0

# Test fixtures (Phase 5, generate real PDFs for extraction tests)
reportlab>=4.0.0
Pillow>=10.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
coverage>=7.4.0

# Code quality
ruff>=0.15.21
mypy>=1.10.0
